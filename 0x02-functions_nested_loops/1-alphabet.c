#include "main.h"

/**
 * prints_alphabet - prints the alphabet in lowercase  followed by a new line.
 */
void print_alphabet(void)
{
	char a;

	for (a = 'a'; a <= 'z'; letter++)
		_putchar(a);

	_putchar('\n');
}
